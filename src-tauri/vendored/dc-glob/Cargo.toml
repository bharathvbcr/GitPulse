[package]
name = "dc-glob"
version = "0.2.0"
edition = "2024"
license = "Apache-2.0"
description = "Python fnmatch semantics, shared by the harness's analysis plane"

[lints.rust]
unsafe_code = "forbid"

[lints.clippy]
all = "warn"
