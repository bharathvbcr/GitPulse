name: Code Coverage

on:
  push:
    branches: [main, master, develop]
  pull_request:
    branches: [main, master, develop]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  coverage:
    name: Generate Code Coverage Reports
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Install System Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev libgtk-3-dev build-essential curl wget file libxdo-dev libssl-dev libayatana-appindicator3-dev librsvg2-dev pkg-config

      - name: Setup Rust Toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          components: llvm-tools-preview

      - name: Install cargo-llvm-cov
        uses: taiki-e/install-action@cargo-llvm-cov

      - name: Rust Cache
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: src-tauri

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install Frontend Dependencies
        run: npm ci

      - name: Frontend Coverage (Vitest + v8)
        run: npm run coverage

      - name: Verify Frontend LCOV Was Produced
        run: |
          test -f coverage/lcov.info || {
            echo "coverage/lcov.info missing — vitest --coverage did not emit lcov";
            exit 1;
          }

      - name: Upload Frontend Coverage Artifact
        uses: actions/upload-artifact@v4
        with:
          name: frontend-lcov
          path: coverage/lcov.info
          if-no-files-found: error

      - name: Generate Rust Coverage (LCOV + summary)
        run: |
          cargo llvm-cov --manifest-path src-tauri/Cargo.toml --workspace --lcov --output-path lcov.info
          cargo llvm-cov --manifest-path src-tauri/Cargo.toml --workspace --summary-only > rust-coverage-summary.txt
          cat rust-coverage-summary.txt

      - name: Verify Rust LCOV Was Produced
        run: |
          test -f lcov.info || {
            echo "lcov.info missing — cargo llvm-cov did not emit lcov";
            exit 1;
          }

      - name: Enforce Coverage Floors and Validate LCOV
        run: npm run check:coverage

      - name: Upload Rust Coverage Artifact
        uses: actions/upload-artifact@v4
        with:
          name: rust-lcov
          path: lcov.info
          if-no-files-found: error

      - name: Report Coverage Summary
        run: |
          {
            echo "### Code coverage"
            echo
            echo "#### Rust (\`cargo llvm-cov\`)"
            echo '```'
            cat rust-coverage-summary.txt
            echo '```'
            echo
            echo "#### Frontend"
            echo "Vitest v8 coverage artifact: \`coverage/lcov.info\`"
          } >> "$GITHUB_STEP_SUMMARY"
